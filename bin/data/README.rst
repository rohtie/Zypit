Create a directory named `project` inside this directory.
Zypit will use the `project` directory for your shaders, textures and the project file.

It is recommended to track your `project` folder using git.
